<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.upc.miappposta">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiappPosta"
        tools:targetApi="31">
        <activity
            android:name=".SeleccionHorarioActivity"
            android:exported="false" />
        <activity
            android:name=".EspecialidadesActivity"
            android:exported="false" />
        <activity
            android:name=".ConfirmacionActivity"
            android:exported="false" />
        <activity
            android:name=".HistoricoActivity"
            android:exported="false" />
        <activity
            android:name=".AgendaCitaActivity"
            android:exported="false" /> <!-- Pantalla de inicio (Bienvenida) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Pantalla de login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" /> <!-- Pantalla de registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- Pantalla principal tras iniciar sesión -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
    </application>

</manifest>